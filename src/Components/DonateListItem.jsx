import React from "react";
import { useState } from "react";
const DonateListItem = ({ image, title, amount }) => {
  return (
    <div>
      <input type="radio" name="donateItem" />
    </div>
  );
};

export default DonateListItem;
